{
  "name": "<%= _.slugify(projectTitle) %>",
  "version": "0.0.0",
  "dependencies": {<% if (useZepto) { %>
    "zepto": "latest",<% } else { %>
    "jquery": "~1.10.1",<% } %>
    "lodash": "latest",
    "backbone": "latest",
    "requirejs": "latest",
    "handlebars": "latest",
    "modernizr": "latest"
  }
}